// Intentionally empty. All bridge sources live in ../../../native/
// (native/src/git_bridge.c, native/include/git_bridge.h) and are
// pulled in directly by git2_bridge.podspec's `s.source_files` — this
// file exists only so CocoaPods' `Classes/**/*` glob in the podspec
// never matches zero files.
