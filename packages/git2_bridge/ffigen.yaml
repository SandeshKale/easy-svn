# Run with `dart run ffigen --config ffigen.yaml` (from this directory).
# Regenerate whenever ../../native/include/git_bridge.h changes.
name: Git2BridgeBindings
description: |
  Auto-generated FFI bindings for git_bridge.h — the flat C wrapper
  around libgit2 (see ../../native/README.md). Do not hand-edit; run
  `dart run ffigen --config ffigen.yaml` instead.
output: 'lib/src/git2_bridge_bindings_generated.dart'
headers:
  entry-points:
    - '../../native/include/git_bridge.h'
  include-directives:
    - '../../native/include/git_bridge.h'
comments:
  style: any
  length: full
preamble: |
  // Generated by `dart run ffigen --config ffigen.yaml`. Do not edit by hand.
